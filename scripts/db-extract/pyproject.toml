[project]
name = "db-extract"
version = "0.1.0"
description = "Extract data from databases to CSV/XLSX"
requires-python = ">=3.11"
dependencies = [
    "pyyaml>=6.0",
    "pandas>=2.2",
    "openpyxl>=3.1",
    "pymysql>=1.1",
    "python-dotenv>=1.0",
    "sqlalchemy>=2.1.1",
]
