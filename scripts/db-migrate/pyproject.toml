[project]
name = "db-migrate"
version = "0.1.0"
description = "Migrate data between databases"
requires-python = ">=3.11"
dependencies = [
    "pyyaml>=6.0",
    "pandas>=2.2",
    "pymysql>=1.1",
    "python-dotenv>=1.0",
]
