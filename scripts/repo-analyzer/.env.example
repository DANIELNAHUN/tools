# GitHub token for API calls (optional, for private repos)
GITHUB_TOKEN=

# Default path to repos directory (overridden by --path)
# REPOS_PATH=/home/user/projects

# Output directory for reports and backups
OUTPUT_DIR=output
