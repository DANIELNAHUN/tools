[project]
name = "repo-analyzer"
version = "0.1.0"
description = "Analyze git repositories for status, changes, and .env backups"
requires-python = ">=3.11"
dependencies = [
    "python-dotenv>=1.0",
]
